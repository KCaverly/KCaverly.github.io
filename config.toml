# Site settings
description = "kcaverly - Home"

# User settings
# The URL the site will be built for
base_url = "https://KCaverly.github.io"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

theme = 'particle-zola'

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

[extra]
username = "kcaverly"
user_description = "Professionally, I build machines that invest. I am currently responsible for research and analytic systems development for a systematic investment team managing 15 billion. Personally, I love developer tooling, hacking on programming languages, and language models. I like Linux, the terminal, and building machines that read."
user_title = "Software Engineering and Research"
email = "kylebcaverly@gmail.com"
github_username =  "KCaverly"
